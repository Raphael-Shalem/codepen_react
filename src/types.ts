export interface Icode {
    html: string;
    css: string;
    js: string;
}

export type TeditorVariant = "html" | "css" | "js"


export interface Idimentions {
    htmlWidth: number;
    cssWidth: number;
    jsWidth: number;
}